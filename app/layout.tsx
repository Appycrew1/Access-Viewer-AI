import './globals.css'
export const metadata = { title: 'Pre‑Survey — Next.js + AI Plus', description:'Moving pre‑survey add‑on (Next.js, Tailwind, AI‑ready)' }
export default function RootLayout({ children }) { return (<html lang="en" suppressHydrationWarning><body>{children}</body></html>) }
