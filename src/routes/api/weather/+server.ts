import type { RequestHandler } from '@sveltejs/kit'; export const GET:RequestHandler=async({url})=>{ const lat=url.searchParams.get('lat'); const lng=url.searchParams.get('lng'); if(!lat||!lng) return new Response(JSON.stringify({error:'missing_params'}),{status:400}); const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lng}&hourly=temperature_2m,precipitation,wind_speed_10m&current_weather=true`,{cache:'no-store'}); const j=await r.json(); const cw=j.current_weather||{}; const temp=cw.temperature; const wind=cw.windspeed; const cond=([0,1].includes(cw.weathercode))?'Clear':'Cloudy'; const precip=cond==='Clear'?10:50; return new Response(JSON.stringify({ date:new Date().toISOString().slice(0,10), condition:cond, temp_c:temp, wind_kmh:wind, precip_chance_pct:precip, source:'live'}),{headers:{'content-type':'application/json'}}) }
