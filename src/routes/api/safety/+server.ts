import type { RequestHandler } from '@sveltejs/kit'; export const GET:RequestHandler=async({url})=>{ const id=url.searchParams.get('address_id')||''; const hazards:any={cust_1:{width_restriction:'2.0m',one_way:true,crime_risk:'Medium'},cust_2:{one_way:false,crime_risk:'Low'},cust_3:{width_restriction:'2.4m',one_way:true,crime_risk:'Low'}}; return new Response(JSON.stringify(hazards[id]||{}),{headers:{'content-type':'application/json'}}) }
