import type { RequestHandler } from '@sveltejs/kit'; export const GET:RequestHandler=async()=>new Response(JSON.stringify({ openai:!!process.env.OPENAI_API_KEY, google:!!process.env.GOOGLE_API_KEY }),{headers:{'content-type':'application/json'}})
